{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "default window capability configuration",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
